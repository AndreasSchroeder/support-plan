<%= form_for(holiday) do |f| %>
  <% if holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% holiday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Datum" %>
    <%= f.text_field :day, class: "datepicker2" %>
  </div>

  <div class="field">
    <%= f.label "Name" %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Feiertag erstellen", class: "btn btn-lg btn-success" %>
  </div>
<% end %>
